name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  backend:
    name: Backend (.NET)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: "9.0.x"
      - name: Restore
        run: dotnet restore GuildedThorn.com.csproj
      - name: Build
        run: dotnet build GuildedThorn.com.csproj -c Release --no-restore
      - name: Test
        run: dotnet test Tests/GuildedThorn.Tests.csproj -c Release

  frontend:
    name: Frontend (bun)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: GuildedThorn.com-Frontend
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
      - name: Install
        run: bun install
      - name: Lint
        run: bun run lint
      - name: Unit tests
        run: bun run test
      - name: Build
        run: bun run build
